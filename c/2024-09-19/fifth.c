#include <stdio.h>

int main() {
    int first_number;
    int second_number;
    scanf("%d%d", &first_number, &second_number);
    printf("Input numbers: %d, %d.\n", first_number, second_number);
    return 0;
}
