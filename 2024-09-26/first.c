#include <stdio.h>

int main() {
    int first_one = 5;
    int second_one;
    int third_one = 55;
    printf("first_one: %d\nsecond_one: %d\nthird_one: %d\n", first_one, second_one, third_one);
    return 0;
}
